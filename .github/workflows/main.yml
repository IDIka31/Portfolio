name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "4240ba39-15a6-4888-b455-353b44ff7e5d"
          heroku_app_name: "idika-portfolio" #Must be unique in Heroku
          heroku_email: "heroku@idikanugraha.me"
          branch: "main"
